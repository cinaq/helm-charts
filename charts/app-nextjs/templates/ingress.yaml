{{- if and (eq (printf "%v" .Values.ingress.enabled) "true") .Values.ingress.hosts }}
{{- $serviceName := include "app-nextjs.fullname" . -}}
{{- $servicePort := .Values.service.externalPort -}}
{{- $className := .Values.ingress.ingressClassName -}}
{{- $globalAnnotations := .Values.ingress.annotations | default dict -}}
{{- range $host := .Values.ingress.hosts }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-%s" $.Release.Name (required "ingress.hosts[].id is required" $host.id | toString | lower) }}
  labels:
    {{- include "app-nextjs.labels" $ | nindent 4 }}
  {{- $annotations := mustMergeOverwrite (deepCopy $globalAnnotations) ($host.annotations | default dict) }}
  {{- if $annotations }}
  annotations:
    {{- toYaml $annotations | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ if eq (toString $host.visibility) "private" }}nginx-internal{{ else }}{{ $host.ingressClassName | default $className }}{{ end }}
  rules:
    - host: {{ required "ingress.hosts[].host is required" $host.host }}
      http:
        paths:
          - path: {{ $host.path | default "/" }}
            pathType: Prefix
            backend:
              service:
                name: {{ $serviceName }}
                port:
                  number: {{ $servicePort }}
  {{- if and $host.tls $host.tls.secretName (eq (printf "%v" $host.tls.enabled) "true") }}
  tls:
    - hosts:
        - {{ $host.host }}
      secretName: {{ $host.tls.secretName }}
  {{- end }}
{{- end }}
{{- end }}
